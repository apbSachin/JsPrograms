//Program to check if an array is sorted in ascending or descending order.
const array = [12, 1, 23, 45, 2, 8];
function sortingAscDsc(array) {
    //if array length is less then 1, then it should be considered as sorted.
  if (array.length < 1) {
    return "array is sorted";
  }

  let ascending = true;
  let descending = true;
  //start loop with 1 element so we can compare with previous elements 
  for(let i=1; i<array.length; i++) {

  }

}
